# Copy this to .streamlit/secrets.toml for LOCAL testing with `st.secrets`,
# OR paste the same key/value into the Streamlit Community Cloud dashboard
# under: App settings -> Secrets.
#
# NEVER commit the real .streamlit/secrets.toml file — it's in .gitignore.

DATABASE_URL = "postgresql://postgres:<your-password>@db.<your-project-ref>.supabase.co:5432/postgres"
SUPER_ADMIN_SEED_USERNAME = "super_admin"
SUPER_ADMIN_SEED_PASSWORD = "ChangeMe_On_First_Login!"
APP_NAME = "NABA Tech QC"
CREDIT_LINE = "Powered by NABA Tech By Kaleem Ullah Sharif"
